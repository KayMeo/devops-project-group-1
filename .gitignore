# Dependency directories (Rất quan trọng - chặn 3000+ file rác)
node_modules/
jspm_packages/

# Build outputs (Chặn các file đã biên dịch)
dist/
build/
.next/
out/

# Environment variables (Bảo mật - không đẩy mật khẩu database lên)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker (Không đẩy dữ liệu database local lên)
postgres_data/

# OS metadata
.DS_Store
Thumbs.db